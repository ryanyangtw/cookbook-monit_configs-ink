check system localhost
  if loadavg (1min) > <%= node['monit']['system']['loadavg_for_one_minute'] %> then alert
  if loadavg (5min) > <%= node['monit']['system']['loadavg_for_five_minutes'] %>  then alert
  if memory usage > 75% then alert
  if cpu usage (user) > 70% for 5 cycles then alert
  if cpu usage (system) > 30% for 5 cycles then alert
  if cpu usage (wait) > 20% for 5 cycles then alert
